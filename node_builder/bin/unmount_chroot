#!/usr/bin/env bash
set -Eeuo pipefail
set -x

chroot_dir="$1"

mounts=(
"${chroot_dir}/proc"
"${chroot_dir}/sys"
"${chroot_dir}/dev"
)

for mount in "${mounts[@]}"; do
  if mountpoint -q "$mount"; then sudo umount "$mount"; fi
done
