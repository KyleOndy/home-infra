#!/usr/bin/env bash
set -Eeuo pipefail
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

usage() {
  echo "$0 <chroot_dir>"
  echo "    No output"
}

if [[ "$#" -ne 1 ]]; then
  usage
  exit 1
fi

chroot="$1"

"$DIR/bin/unmount_chroot" "$chroot"

sudo rm -r "$chroot"
