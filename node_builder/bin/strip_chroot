#!/usr/bin/env bash
set -Eeuo pipefail

usage() {
  echo "$0 <chroot>"
  echo "    output: path to ramroot.tar.gz"
}

if [[ "$#" -ne 1 ]]; then
  usage
  exit 1
fi

chroot="$1"

sudo rm -r "$chroot/boot/"*
